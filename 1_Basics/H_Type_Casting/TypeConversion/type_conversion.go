package main

import (
	"fmt"
	// "math"
)

func main() {
	// i := 42
	// f := float64(i)
	// u := uint(f)

	// var x, y int = 7, 8
	// var f float64 = math.Sqrt(float64(x*x + y*y))
	// var z uint = uint(f)
	// fmt.Println(x, y, f, z)

	p:=23;
	fmt.Printf("type of p: %T\n", p)
	q:= float32(p)
	fmt.Printf("type of q: %T\n", q)
}
